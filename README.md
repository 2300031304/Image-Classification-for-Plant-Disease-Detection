# Image-Classification-for-Plant-Disease-Detection
This project implements a Convolutional Neural Network (CNN) to automatically detect and classify plant leaf diseases from images. The model is trained on the Plant Leaf Disease dataset from Kaggle and achieves an accuracy of 96%. Image preprocessing and data augmentation techniques are applied to improve performance. 
